{
  "name": "koa-route-class",
  "version": "0.3.0",
  "description": "Class wrapper for koa-route with nesting support.",
  "main": "lib/index.js",
  "scripts": {
    "compile": "babel -d lib/ src/",
    "prepublish": "npm run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Richard <hello@noxan.net>",
  "license": "MIT",
  "dependencies": {
    "koa-route": "^3.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-polyfill": "^6.7.4",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
